- name: "Write Nginx site config for {{ application_name }}"
  template:
    src: website.conf
    dest: "/etc/nginx/sites-available/{{ application_name }}.conf"
    mode: '0644'
    owner: root
    group: root

- name: Enable Nginx site config for SPA
  file:
    src: "/etc/nginx/sites-available/{{ application_name }}.conf"
    dest: "/etc/nginx/sites-enabled/{{ application_name }}"
    mode: '0644'
    owner: root
    group: root
    state: link

- name: Restart Nginx
  service:
    name: nginx
    state: restarted

- name: Run LetsEncrypt
  command: certbot --nginx -d {{ application_domain_name }} -d www.{{ application_domain_name }} --expand --non-interactive --agree-tos -m d4vid81@gmail.com
